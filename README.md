README

tokenizer.py — Core Tokenizer/Scanner implementation for Core language.

documentation.txt — Design overview, user manual, testing notes.

How to run:

Requires Python 3.

Run: python3 tokenizer.py <inputfile>

Notes:

Outputs one token number per line, then prints an EOF or error message and exits.
